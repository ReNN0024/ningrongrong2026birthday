# Logo 坐标系二次赋值说明

本文档描述如何对部分自带倾向的 Logo 进行坐标系二次赋值，以细化结果判定。

---

## 一、坐标系二次赋值概念

### 1.1 默认坐标系

默认情况下，坐标系的 X 轴分界点为原点 (0, 0)：

- **荆棘（thorn）**：x ∈ [-2, 0)
- **繁花（flower）**：x ∈ [0, 2]
- **不忘（memory）**：y ∈ [0, 2]
- **寻常（daily）**：y ∈ [-2, 0)

Logo 的实际放置范围为 x ∈ [-1, 1]，y ∈ [-1, 1]，对应上述概念坐标的半程。

### 1.2 二次赋值

部分 Logo 本身带有强烈的倾向性（如"暝夜"自带荆棘属性），若按默认坐标系判定，会失去细分意义。

**二次赋值**通过偏移分界点，让 Logo 在自身倾向范围内也能产生差异化判定。

以下为概念示例（非实际配置），用于说明二次赋值的效果：

| 维度 | 默认分界 | 二次赋值（示例） |
|---|---|---|
| X 轴（荆棘/繁花） | 0 | **-1** |
| Y 轴（不忘/寻常） | 0 | 0（不变） |

- 默认：荆棘 = [-2, 0)，繁花 = [0, 2]
- 二次赋值后：荆棘 = [-2, -1)，繁花 = [-1, 2]

**效果**：当用户把某 Logo 放在 x = -0.5 时：
- 默认判定 → 荆棘（因为 -0.5 < 0）
- 二次赋值判定 → 繁花（因为 -0.5 > -1）

这样，即使 Logo 本身带有荆棘倾向，用户仍可通过放置位置表达"相对繁花"的意图。

---

## 二、二次赋值对照表

仅列出有二次赋值的 Logo。未出现在此表的 Logo 均使用默认坐标系。

| Logo 编号 | Logo 名称 | X 轴分界偏移 | Y 轴分界偏移 | 说明 |
|---|---|---|---|---|
| `4_3` | 暝夜 | -1 | 1 | X 分界左移 -1，Y 分界上移 1（荆棘/不忘范围收窄） |
| `5_1` | 莲花去国一千年 | -1 | 1 | X 分界左移 -1，Y 分界上移 1（荆棘/不忘范围收窄） |
| `5_4` | 海落潮升 | -1 | 0 | X 轴分界左移至 -1（荆棘范围收窄） |
| `8_3` | 再加九克好奇心 | -1 | 0 | X 轴分界左移至 -1（荆棘范围收窄） |
| `1_1` | 致绽放的你 | 1 | 0 | X 轴分界右移至 1（繁花范围收窄） |
| `2_1` | 锋芒 | 1 | 1 | X 分界右移 1，Y 分界上移 1（繁花/不忘范围收窄） |
| `4_4` | 心动瞬间 | 1 | 0 | X 轴分界右移至 1（繁花范围收窄） |
| `6_3` | 现实童话 | 1 | 0 | X 轴分界右移至 1（繁花范围收窄） |
| `3_1` | 冲调午后 | 0.5 | -1 | X 分界右移 0.5，Y 分界下移 -1（繁花/寻常范围收窄） |
| `3_2` | 左满舵 | 0.5 | -1 | X 分界右移 0.5，Y 分界下移 -1（繁花/寻常范围收窄） |
| `3_3` | 槐花冰奶七分糖 | 0.5 | -1 | X 分界右移 0.5，Y 分界下移 -1（繁花/寻常范围收窄） |
| `2_2` | 知晓我在的人 | 0.5 | 0 | X 轴分界右移至 0.5（繁花范围收窄） |
| `7_2` | 直到世界听到 | 0.5 | 0 | X 轴分界右移至 0.5（繁花范围收窄） |
| `5_3` | 引梦渡海 | 1 | 0 | X 轴分界右移至 1（繁花范围收窄） |
| `7_1` | 伴生 | 1 | 0 | X 轴分界右移至 1（繁花范围收窄） |

### 字段说明

- **X 轴分界偏移**：荆棘/繁花的分界点。默认值为 0，负值表示分界左移（荆棘范围缩小），正值表示分界右移。
- **Y 轴分界偏移**：不忘/寻常的分界点。默认值为 0，负值表示分界下移（不忘范围缩小），正值表示分界上移。

### 添加新 Logo 的格式

```markdown
| `{行}_{列}` | {名称} | {X 偏移} | {Y 偏移} | {说明} |
```

示例：若 `3_5 烈焰` 自带繁花倾向，X 轴分界右移至 0.5：

```markdown
| `3_5` | 烈焰 | 0.5 | 0 | 自带繁花倾向，X 轴分界右移至 0.5 |
```

---

## 三、对结果输出的影响

### 3.1 主倾向判定（象限归属）

默认情况下，Logo 的象限归属由坐标 (x, y) 的正负决定：

- 第一象限（N）：x ≥ 0, y ≥ 0
- 第二象限（O）：x < 0, y ≥ 0
- 第三象限（R）：x < 0, y < 0
- 第四象限（G）：x ≥ 0, y < 0

**二次赋值后**，使用偏移后的分界点判定：

```javascript
// 默认
const quadrantX = item.x >= 0 ? "right" : "left";
const quadrantY = item.y >= 0 ? "up" : "down";

// 二次赋值（以暝夜为例，xOffset = -1）
const quadrantX = item.x >= xOffset ? "right" : "left";
const quadrantY = item.y >= yOffset ? "up" : "down";
```

### 3.2 副倾向判定（坐标加权）

副倾向由所有 Logo 的坐标总和决定：

- `sumX` = Σ(item.x)
- `sumY` = Σ(item.y)

**二次赋值后**，使用偏移后的坐标计算：

```javascript
// 默认
sumX += item.x;
sumY += item.y;

// 二次赋值（以暝夜为例，xOffset = -1）
sumX += item.x - xOffset;  // 相当于 item.x - (-1) = item.x + 1
sumY += item.y - yOffset;
```

**效果**：暝夜放在 x = -0.5 时，对 sumX 的贡献从 -0.5 变为 +0.5，更倾向繁花。

### 3.3 结果图生成

结果图的主倾向系列（R/O/N/G）和底图颜色由主倾向决定，不受二次赋值影响。

二次赋值仅影响：
1. Logo 的象限归属（主倾向统计）
2. 坐标加权总和（副倾向判定）

---

## 四、设计原则

1. **保守使用**：仅对自带强烈倾向的 Logo 进行二次赋值，避免过度干预用户意图。
2. **单向偏移**：通常只偏移一个轴（X 或 Y），保持另一轴不变。
3. **偏移范围**：建议在 [-1, 1] 范围内，避免分界点超出 Logo 放置范围。
4. **文档同步**：每次添加/修改二次赋值，需同步更新本表和 `share-card.js` 中的 `LOGO_QUADRANT_OFFSETS` 配置。
