# 2026.07.16 17:22 更新日志

## 本次修改内容

### 1. 坐标系左右对称布局
- `.coordinate-editor` 改为三列 Grid：`72px | 1fr | 72px`
- 左侧 72px 空白列与右侧工具轨等宽，坐标系居中对称
- `.coordinate-frame` 指定 `grid-column: 2`
- `.coordinate-side-tools` 指定 `grid-column: 3`

### 2. 坐标系与工具轨间距
- `column-gap: 8px`，精确控制坐标系到工具栏的距离
- 工具栏和导航框左边缘对齐（`align-items: flex-start`）

### 3. 坐标轴文字居中
- `.axis-top` / `.axis-bottom` 增加 `align-items: center` 和 `text-align: center`

### 4. 移动端隐藏 FRAGMENTS
- 移动端媒体查询中 `.library-head .section-index { display: none }`

### 5. 移动端长按三段式反馈
- **按下**：Logo 下方出现 280ms 金色进度条
- **长按成立**：轻振动 + Logo 向上浮起并放大（scale 1.08）
- **拖动中**：跟手图位于手指上方 18px，进入坐标系后中央显示"松开放置"
- 删除了跟手图下方的重复文字提示

### 6. 左栏四段等距布局
- PC 端 `.guide-panel` 改为 CSS Grid + `align-content: space-between`
- 四个区块：操作指引 / 怎样读坐标 / 小提示 / 进度+Credits
- 三个间距自动平均分配剩余高度，最小保持 8px
- HTML 中标题+简介+三步操作包入 `.guide-primary` 容器
- 清除各组件原有的固定 margin，避免间距叠加

### 7. 清理跟手提示（补充确认）
- `app.js` `startDrag()`：`dom.ghost.innerHTML` 仅保留 `mediaMarkup(logo)`，不再附加 tip 元素
- `app.js` `updateDrag()`：删除 `.drag-ghost-tip` 查询和文字切换逻辑
- `styles.css`：彻底删除 `.drag-ghost-tip { ... }` 整段样式
- 保留坐标系中央 `#dropFeedback > span "松开放置"` 作为唯一的放置反馈

---

**涉及文件**：`index.html`、`styles.css`、`app.js`
